<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  <style>

#navbar {
  font-family: "Trebuchet MS", Arial, Helvetica, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

#navbar th {
  padding-top: 12px;
  padding-bottom: 12px;
  text-align: center;
  background-color: #bdddaa;
  color: white;
}

</style>
</head>
<body>


<table id="navbar">
  <tr>
    <th><a href="/users/dashboard/<%= @user.id %>">Dashboard</a>
    <th><a a href="/users/logout">Log Out</a></th>
  </tr>
  </table>


<br>
<br>
<br>

<h2>Welcome to your dashboard, <%=@user.name%>!</h2>
<p>Click on any project for more information</p>

<br>
<h1>My Projects</h1>
<br>
  <%if @projects %>
    <% if @projects.size == 1 %>
    <ul>
      <li><a href="/projects/<%= @projects.first.id %>"><%= @projects.first.name.capitalize %></a></li>
    </ul>
    <% elsif @projects.size > 1 %>
      <% @projects.each do |project| %>
      <ul>
        <li><a href="/projects/<%= project.id %>"><%= project.name.capitalize %></a></li>
      </ul>
      <% end %>
    <% end %>
  <% end %>
  <!-- todo: render flash message if no projects-->

  <br>
  <br>
  <a href="/projects/new"><input type="submit" value="Create New Project"></a>
  <br>
  <br>
  <br>

  <h1>My Supplies</h1>
  <br>

    <% @user.projects.each do |project| %>
      <% s = project.supplies %>
      <% s.uniq.each do |supply| %>
        <a href="/supplies/<%= supply.id %>"><%= supply.name %></a>
        <br>
      <% end %>
    <% end %>

    <br>
    <br>
    <a href="/supplies/new"><input type="submit" value="Create New Supplies"></a>
    <br>
    <br>
    <br>

  </body>
